apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ai-postgres-pvc
  namespace: apex
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ai-postgres
  namespace: apex
  labels:
    app: ai-postgres
spec:
  serviceName: ai-postgres
  replicas: 1
  selector:
    matchLabels:
      app: ai-postgres
  template:
    metadata:
      labels:
        app: ai-postgres
    spec:
      containers:
        - name: postgres
          image: pgvector/pgvector:pg17
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: ai-postgres-secret
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB"]
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: ai-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: ai-postgres
  namespace: apex
spec:
  selector:
    app: ai-postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
---
apiVersion: v1
kind: Secret
metadata:
  name: ai-postgres-secret
  namespace: apex
type: Opaque
stringData:
  POSTGRES_DB: ai_service
  POSTGRES_USER: ai-service
  POSTGRES_PASSWORD: change-me-in-production
